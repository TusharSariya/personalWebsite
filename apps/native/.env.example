# Local dev: mock mode (no API or database)
EXPO_PUBLIC_USE_MOCK_FEED=true
EXPO_PUBLIC_USE_MOCK_AUTH=true

# Real API (point at deployed server Worker URL for device testing)
# EXPO_PUBLIC_USE_MOCK_FEED=false
# EXPO_PUBLIC_USE_MOCK_AUTH=false
# EXPO_PUBLIC_SERVER_URL=https://personalWebsite-server-prod.your-account.workers.dev

# Use your Mac LAN IP (not localhost) when testing on a physical phone
EXPO_PUBLIC_SERVER_URL=http://localhost:3000
