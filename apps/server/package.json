{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server"
  },
  "dependencies": {
    "@orpc/openapi": "catalog:",
    "@orpc/server": "catalog:",
    "@orpc/zod": "catalog:",
    "@personalWebsite/api": "workspace:*",
    "@personalWebsite/auth": "workspace:*",
    "@personalWebsite/db": "workspace:*",
    "@personalWebsite/env": "workspace:*",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@personalWebsite/config": "workspace:*",
    "@types/node": "^22.13.14",
    "alchemy": "catalog:",
    "tsdown": "^0.21.9",
    "typescript": "catalog:",
    "wrangler": "catalog:"
  },
  "trustedDependencies": [
    "supabase"
  ]
}
